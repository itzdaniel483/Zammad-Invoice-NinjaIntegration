version: '3.8'

services:
  zammad-invoiceninja:
    build: .
    image: zammad-invoiceninja-webhook:latest
    container_name: zammad-invoiceninja-webhook
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - invoiceninja-data:/app/data

volumes:
  invoiceninja-data:
